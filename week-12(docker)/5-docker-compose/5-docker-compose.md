<!-- Use of docker compose -->

- It is used to run multiple service(conatiner) using a single compose file.
- Benefit :
  - Developer don't have to run individual container by themself, sinlge command all the service is up and running based on configuration set on `compose.yml` file.
